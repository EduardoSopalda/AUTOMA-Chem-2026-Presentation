# Opening title card — AUTOMA Chem 2026

Open `index.html` in Chrome or Edge. Press F for fullscreen.

- Space pauses and resumes the drawing
- Click replays from the start
- Fonts are local (`fonts/`) so it runs offline after the first copy to a USB stick

All original lines stay on the plate. Type is live, not painted into the drawing, so the hierarchy can hold a room.
